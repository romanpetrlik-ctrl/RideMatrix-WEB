<!DOCTYPE html>
<html lang="en">
  <%
    const customerBreadcrumbs = [
      { label: "Administration", href: "/dashboard" },
      { label: "Customer management", href: backToCustomersHref },
      { label: `${customer.surname}, ${customer.givenName}` }
    ];
  %>
  <%- include("../../partials/head", { title, appTitle }) %>
  <body>
    <%- include("../../partials/system-status-bar", {
      appTitle,
      systemStatus: "System status: Healthy",
      currentUserEmail: email,
      currentUserHref: "/account"
    }) %>

    <%- include("../../partials/header", {
      appTitle,
      pageTitle: `${customer.surname}, ${customer.givenName}`,
      pageMeta: `Created ${customer.formattedCreatedAt} · Last login ${customer.formattedLastLoginAt}`,
      activeRoleLabel,
      hintsLabel: customer.status,
      breadcrumbs: customerBreadcrumbs,
      contextBarPartial: "header-context/customer-detail",
      contextBarData: { customer, backToCustomersHref, isChildWindow }
    }) %>

    <main class="container">
      <% if (notice) { %>
        <section class="notice notice--<%= notice.tone %>">
          <%= notice.message %>
        </section>
      <% } %>

      <%- include("../../partials/customer-detail-content", { customer, mapView, mapBrowserApiKey, mapId }) %>
    </main>

    <%- include("../../partials/footer") %>
  </body>
</html>

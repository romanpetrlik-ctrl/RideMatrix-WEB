<%- include("../layouts/base", {
  title,
  appTitle,
  currentUserEmail: email,
  currentUserHref: "/account",
  pageTitle: roleLabel,
  pageMeta: "Role workspace",
  activeRoleLabel: roleLabel,
  hintsLabel: "Hints: Off",
  showRoleSwitch: true,
  showExitLink: true,
  body: `
    <section class="panel">
      <h1>${roleLabel}</h1>
      <p><strong>Signed in as:</strong> ${email}</p>
      <p>This module is available to your authorized internal account.</p>
      ${module.key === "manage-vps" ? `
        <div class="notice notice--warning">
          <strong>Safety notice:</strong> reboot and recovery are placeholders and cannot execute VPS actions.
        </div>
        <p><a href="/recovery">Open reboot and recovery placeholder</a></p>
      ` : ""}
      <p><a href="/choose-role">Switch workspace</a></p>
    </section>
  `
}) %>
